import React from 'react';
import { Navigate } from 'react-router-dom';

const Cart: React.FC = () => {
  // Redirigir a la implementación completa del carrito
  return <Navigate to="/carrito" replace />;
};

export default Cart;