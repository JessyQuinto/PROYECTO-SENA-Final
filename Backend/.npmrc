# Prevent workspace resolution during CI/CD
workspaces=false
# Skip lifecycle scripts that might cause issues in production
ignore-scripts=false
# Ensure we only install production dependencies
production=false